export default function App() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Extension workspace</h1>
      <p>
        This preview app exists only so the Lovable project can build.
        Your extension source is in <code>scripts/</code>, <code>html/</code>, and related folders.
      </p>
    </main>
  );
}
